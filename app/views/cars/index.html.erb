<h1>🚗 All Cars</h1>

<ul>
  <% @cars.each do |car| %>
    <li>
      <strong><%= car.brand %> <%= car.model %></strong> —
      <%= car.year %> —
      <%= car.fuel.capitalize %> —
      Owner: <%= car.owner.nickname %>
    </li>
  <% end %>
</ul>
